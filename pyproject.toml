[project]
name = "schelling1971-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "schelling-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
schelling-tools = { workspace = true }
